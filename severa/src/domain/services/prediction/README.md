# domain/services/prediction (M-16) 🔒

M-16 está bloqueado — pendiente de material académico. No implementar ni sugerir un método hasta recibirlo.

El stub del puerto de salida (`IMotorPrediccion`) ya existe en `application/ports/out/prediccion/`, con su implementación stub en `infrastructure/adapters/out/prediccion/MotorPrediccionBloqueado.ts`. Esta carpeta (lógica de dominio pura) queda reservada para cuando exista un algoritmo de predicción real que documentar aquí.
