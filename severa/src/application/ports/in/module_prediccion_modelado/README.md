# module_prediccion_modelado (M-16) 🔒

M-16 está bloqueado — pendiente de material académico. No implementar ni sugerir un método hasta recibirlo.

El stub del puerto de salida (`IMotorPrediccion`) ya existe en `application/ports/out/prediccion/`. Esta carpeta (puertos de entrada) queda reservada para cuando exista un caso de uso real que exponer.
