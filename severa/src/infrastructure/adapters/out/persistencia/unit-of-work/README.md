# persistencia/unit-of-work

El documento de arquitectura reserva esta carpeta para la implementación del patrón unit-of-work, usado cuando una operación necesita coordinar varios repositorios de forma transaccional. Hoy ningún caso de uso lo necesita, así que no hay ninguna implementación real que mover aquí todavía.

Carpeta reservada para cuando exista una operación real que la necesite. Ver también `application/ports/out/persistencia/unit-of-work/` (el puerto correspondiente, en el mismo estado).
