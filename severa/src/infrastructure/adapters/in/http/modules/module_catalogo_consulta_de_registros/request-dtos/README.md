# module_catalogo_consulta_de_registros (M-04) — request-dtos

Este módulo (M-04, "Catálogo y Consulta de Registros") no tiene implementación propia hoy — la funcionalidad de consulta que existía (`ConsultarVulnerabilidadPorCVE`, filtros por rango/categoría) se reclasificó como M-09 ("Priorización y Framework de Clasificación Configurable"), ver sección IV/V de `docs/Arquitectura de SEVERA.md`.

Carpeta reservada para cuando se desarrolle contenido específico de M-04.
